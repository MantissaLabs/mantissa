# mantissa-dev

arch: "aarch64"
vmType: "qemu"
cpus: 8
memory: "16GiB"
disk: "100GiB"

images:
  - location: "https://cloud.debian.org/images/cloud/bookworm/latest/debian-12-generic-arm64.qcow2"
    arch: "aarch64"

mountType: "9p"
mounts:
  - location: "~"
    writable: true

# To configure the terminal with ghostty:
#   infocmp -x | ssh -i ~/.lima/_config/user -p 7183 <username>@127.0.0.1 -- tic -x -
ssh:
  localPort: 7183

provision:
  - mode: user
    script: |
      set -euxo pipefail
      sudo apt-get update && sudo apt-get upgrade -y
      sudo apt-get install -y build-essential curl git capnproto libssl-dev
      # Install rustup and Rust toolchain if not already installed.
      if ! command -v rustup >/dev/null 2>&1; then
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
      fi
      # Ensure that cargo and rustup are added to the PATH for interactive shells.
      if ! grep -q 'cargo/bin' ~/.bashrc; then
        echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> ~/.bashrc
      fi
      if ! grep -q 'cargo/bin' ~/.profile; then
        echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> ~/.profile
      fi

[package]
name = "mantissa"
version = "0.1.0"
description = "Distributed scheduler and cluster management"
license = "Apache-2.0"
repository = "https://github.com/MantissaLabs/mantissa"
homepage = "https://mantissa.io"
readme = "README.md"
license-files = ["LICENSE"]
binaries = ["mantissa", "mantissa-sandbox-init"]
out-dir = "../../target/dist/mantissa"
build-command = ["../../scripts/dist-build.sh"]
